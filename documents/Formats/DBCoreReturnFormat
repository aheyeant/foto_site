DB return formats:
    update, add:
        { "success" => bool,
            if (true)  "object" => "SUCCESS",
            if (false) "object" => log
        }

    delete:
        { "success" => bool",
          "object" => log
        }